"""AHAL AI backend — Increment 1: repository connection + indexing skeleton.

Wraps the existing, tested `ahal` engine (structural graph, co-change index,
extraction) behind a FastAPI service. No prediction or diagnosis logic lives
here yet — see docs/roadmap.md for what's built vs. planned per whitepaper
stage.
"""
